# local state - never commit (holds accounts, chats, API keys)
chat.db
chat.db-wal
chat.db-shm
data/
local.env
*.env
HANDOFF.md

# python
__pycache__/
*.pyc

# node (playwright-core, used only to drive the UI in a throwaway container)
node_modules/
package.json
package-lock.json

# editor / dropbox / runtime cruft
*.bak
*.tmp.*
*.log
.server_pid.txt
.tmp_pid.txt
_page.js

# db backups (full copies of chat.db)
backups/
